# Changesets

Run `npm run changeset` for changes that should publish a package version. The release workflow collects these files into a reviewable version pull request, then publishes that pull request's version through npm trusted publishing after it is merged.

See [the release guide](../CONTRIBUTING.md#releases) for maintainer setup and policy.
